#include <stdio.h>
#include <stdlib.h>

int main()
{
    int age = 40;
    float gpa = 3.6;
    char grade = 'A';
    char phrase[] = "Lucky Charms"; //special type of data
    printf("the age is %d and the gpa is %f and the grade is %c and the phrase is %s", age, gpa, grade, phrase);
    return 0;
}